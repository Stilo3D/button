//import { render, screen, waitFor } from "@testing-library/react";

//import App from "../../App";

test("should render title", async () => {
  // render(<App messageData={undefined}/>);
  // await waitFor(() => {
  //   const titleEl = screen.queryByText("Element Component");
  //   expect(titleEl).toBeInTheDocument();
  // });
});

test("Should render an error message in the browser", async () => {
  // render(<App messageData={}/>);
  // // 1 - set error message
  // // 2 - check that the error test has been rendered to the browser.
  // await waitFor(() => {});
});
